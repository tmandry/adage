- Change const char*s to std::strings -- Use const std::string& in function parameters
- Add comments! Both before each function (use doxygen), and in the code itself
- Implement exceptions (?)
- Change cout to cerr for errors
